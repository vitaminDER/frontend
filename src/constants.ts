export enum PATH {
    BASE = "/",
    BOOKS = "/books",
    REGISTRATION = "/registration",
    AUTH = "/auth",
    BOOK = "/book",
    BOOKITEM = "/book/:id",
    PROFILE = "profile",
    NOT_FOUND = "*",
}

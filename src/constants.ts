export enum PATH {
    BASE = "/",
    BOOKS = "/books",
    REGISTRATION = "/registration",
    AUTH = "/auth",
    BOOK = "/book",
    BOOKITEM = "/book/:id",
    TEST_PAGER = "test_page",
    NOT_FOUND = "*",
}
